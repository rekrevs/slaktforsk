# T-0026: Consolidation pass over the 16 depth-5 dossiers on Sverker's side

## Context

The next edition cohort (depth 5 from Adam and Axel) has 16 people on Sverker's
side. Only those consolidated in `genealogy/identity-review-2026-09-04.md`
carry `GRANSKAD`; the rest are `EJ GRANSKAD`. This task runs the identity
and consolidation pass of `genealogy/research-plan.md` over every dossier
on side S without new source collection.

## Acceptance Criteria

- [ ] All 16 dossiers on side S at depth 5 carry `Konsolidering: GRANSKAD`
  with date and a reference to the research-log entry or identity review
  that records the pass.
- [ ] Observations and conclusions are separated; duplicates, conflicts and
  competing identities are resolved or explicitly kept apart; dependent
  people were re-checked where an identity changed.
- [ ] `goal-state --depth=5` shows side S reviewed = 16.

## Verification Plan

### Hard Endpoints

- [ ] `node scripts/goal-state.mjs --depth=5`
- [ ] `node scripts/validate-genealogy.mjs`
- [ ] `node --test scripts/` and dashboard `npm test`

### Manual Checks

- [ ] No `LEAD`/`CONFLICT` relation was promoted without evidence.

### Risks

- Treating the pass as a checkbox; each dossier needs a recorded reasoning
  line in the research log.

## Approach

Four dossiers per batch; log once per batch.

## Implementation Notes

## Obstacles

## Verification Evidence

## Outcome

Pending.
